// TADAWUL FAST BRIDGE — MANUAL REFRESH PRIORITY COORDINATOR v1.1.0
// v1.1.0 WHY — CG-9, 2026-08-01, WINDOW-SAFE.
// ROOT CAUSE / LIVE EVIDENCE: Global_Markets was observed at 1400/6190 while the
// manual pause was written once for 20 minutes, so it could expire during an
// hours-long manual refresh and permit an automatic/manual writer collision.
// FIX: renewable 20-minute idle lease, immutable six-hour hard ceiling, and P1
// hardening that passes an owner-bound lease context to the configured handler.
// A persisted partial/checkpoint result renews the lease and queues only the
// coordinator-owned one-shot continuation, making renewal executable at the
// post-write/post-verification/post-checkpoint boundary rather than documentation.
// BLAST RADIUS: coordinator pause/continuation state only; no provider, score,
// rank, recommendation, portfolio, Sheet-write implementation, trigger install,
// unrelated-trigger ownership, or live GAS deployment change.
// REVERSIBILITY: stop using lease-context/checkpoint continuation; emergency
// clear, unique-trigger cleanup, automatic yield points and legacy handlers remain.
// Historical contract marker: TFB_REFRESH_COORDINATOR_VERSION = '1.0.2'.
// This file is inert until deployed to the bound Apps Script project.
/* Caller must hold the coordinator DocumentLock */
var TFB_REFRESH_COORDINATOR_VERSION='1.1.0';var TFB_REFRESH_COORDINATOR_=Object.freeze({PROP_MANUAL_UNTIL_MS:'TFB_MANUAL_REFRESH_UNTIL_MS',PROP_MANUAL_REQUEST_ID:'TFB_MANUAL_REFRESH_REQUEST_ID',PROP_MANUAL_REQUESTED_AT:'TFB_MANUAL_REFRESH_REQUESTED_AT',PROP_MANUAL_REQUESTED_BY:'TFB_MANUAL_REFRESH_REQUESTED_BY',PROP_MANUAL_REASON:'TFB_MANUAL_REFRESH_REASON',PROP_MANUAL_HANDLER:'TFB_MANUAL_REFRESH_HANDLER',PROP_AUTO_RESUME_HANDLER:'TFB_AUTO_RESUME_HANDLER',PROP_DEFERRED_TRIGGER_ID:'TFB_MANUAL_DEFERRED_TRIGGER_ID',PROP_LAST_EVENT:'TFB_REFRESH_COORDINATOR_LAST_EVENT',MANUAL_PAUSE_TTL_MS:20*60*1000,MANUAL_PAUSE_MAX_TOTAL_MS:6*60*60*1000,MANUAL_LOCK_WAIT_MS:25*1000,AUTO_LOCK_WAIT_MS:1000,COORDINATOR_LOCK_WAIT_MS:5000,DEFERRED_DELAY_MS:60*1000,AUTO_RESUME_DELAY_MS:60*1000});
function tfbNowMs_() {return Date.now();}
function tfbScriptProperties_() {return PropertiesService.getScriptProperties();}
function tfbNewManualRequestId_() {try{return Utilities.getUuid();}catch(err){return String(tfbNowMs_())+'-'+String(Math.random()).slice(2);}}
function tfbSafeUserEmail_() {try{return Session.getActiveUser().getEmail()||'unknown';}catch(err){return 'unknown';}}
function tfbWithCoordinatorLock_(label, callback) {if(typeof callback!=='function'){throw new Error('tfbWithCoordinatorLock_ requires a callback');}var lock=LockService.getDocumentLock();if(!lock){throw new Error('DocumentLock unavailable; coordinator requires a bound script');}if(!lock.tryLock(TFB_REFRESH_COORDINATOR_.COORDINATOR_LOCK_WAIT_MS)){throw new Error('Coordinator lock busy: '+String(label||'unknown'));}try{return callback();}finally{lock.releaseLock();}}
function tfbRecordRefreshEvent_(eventName, detail) {var payload={version:TFB_REFRESH_COORDINATOR_VERSION,event:String(eventName||'unknown'),detail:String(detail||''),at:new Date().toISOString()};try{tfbScriptProperties_().setProperty(TFB_REFRESH_COORDINATOR_.PROP_LAST_EVENT,JSON.stringify(payload));}catch(err){console.warn('[REFRESH-COORDINATOR] unable to record event: '+err);}console.log('[REFRESH-COORDINATOR] '+JSON.stringify(payload));}
function tfbInactivePause_() {return{active:false,requestId:'',untilMs:0,remainingMs:0,startedAtMs:0,maxUntilMs:0,ceilingRemainingMs:0,ceilingReached:false,reason:'',requestedBy:''};}
function tfbDeleteManualPauseUnlocked_(props) {props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS);props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID);props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_AT);props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_BY);props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REASON);}
function tfbManualPauseStartMsUnlocked_(props, requestId, nowMs) {var requestedId=String(requestId||'');var currentId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var rawStartedAt=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_AT)||'';var parsedStartedAt=Date.parse(rawStartedAt);if(requestedId&&currentId===requestedId&&isFinite(parsedStartedAt)){return parsedStartedAt;}return Number(nowMs||tfbNowMs_());}
function tfbWriteManualPauseUnlocked_(props, reason, requestId) {var nowMs=tfbNowMs_();var requestIdText=String(requestId||'');var previousId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var startedAtMs=tfbManualPauseStartMsUnlocked_(props,requestIdText,nowMs);var maxUntilMs=startedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;var untilMs=Math.min(nowMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_TTL_MS,maxUntilMs);if(untilMs<=nowMs){if(previousId===requestIdText){tfbDeleteManualPauseUnlocked_(props);}return{requestId:requestIdText,untilMs:nowMs,startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,ceilingRemainingMs:0,ceilingReached:true,expired:true};}var requestedBy=previousId===requestIdText?(props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_BY)||''):'';var values={};values[TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS]=String(untilMs);values[TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID]=requestIdText;values[TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_AT]=new Date(startedAtMs).toISOString();values[TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_BY]=requestedBy||tfbSafeUserEmail_();values[TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REASON]=String(reason||'manual-refresh');props.setProperties(values,false);return{requestId:requestIdText,untilMs:untilMs,startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,ceilingRemainingMs:Math.max(0,maxUntilMs-nowMs),ceilingReached:untilMs>=maxUntilMs,expired:false};}
function tfbClearExpiredManualPauseIfMatch_(expectedRaw, expectedRequestId) {var cleared=tfbWithCoordinatorLock_('clear-expired-pause',function(){var props=tfbScriptProperties_();var currentRaw=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS)||'';var currentRequestId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';if(currentRaw !== String(expectedRaw || '')||currentRequestId !== String(expectedRequestId || '')){return false;}var nowMs=tfbNowMs_();var currentUntilMs=Number(currentRaw||0);var startedAtMs=tfbManualPauseStartMsUnlocked_(props,currentRequestId,nowMs);var maxUntilMs=startedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;var effectiveUntilMs=Math.min(currentUntilMs,maxUntilMs);if(isFinite(effectiveUntilMs)&&effectiveUntilMs>nowMs){return false;}tfbDeleteManualPauseUnlocked_(props);return true;});if(cleared){tfbRecordRefreshEvent_('manual-pause-expired','requestId='+String(expectedRequestId||''));}return cleared;}
function tfbReadManualPause_() {for (var attempt = 0; attempt < 3; attempt++){var props=tfbScriptProperties_();var raw=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS)||'';var requestId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var untilMs=Number(raw||0);var nowMs=tfbNowMs_();var startedAtMs=tfbManualPauseStartMsUnlocked_(props,requestId,nowMs);var maxUntilMs=startedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;var effectiveUntilMs=Math.min(untilMs,maxUntilMs);if(isFinite(effectiveUntilMs)&&effectiveUntilMs>nowMs){return{active:true,requestId:requestId,untilMs:effectiveUntilMs,remainingMs:Math.max(0,effectiveUntilMs-nowMs),startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,ceilingRemainingMs:Math.max(0,maxUntilMs-nowMs),ceilingReached:effectiveUntilMs>=maxUntilMs,reason:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REASON)||'',requestedBy:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_BY)||''};}if(!raw){return tfbInactivePause_();}if(tfbClearExpiredManualPauseIfMatch_(raw, requestId)/* State changed while the stale instance was being checked */){return tfbInactivePause_();}}var latestProps=tfbScriptProperties_();var latestRaw=latestProps.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS)||'';var latestRequestId=latestProps.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var latestNowMs=tfbNowMs_();var latestUntilMs=Number(latestRaw||0);var latestStartedAtMs=tfbManualPauseStartMsUnlocked_(latestProps,latestRequestId,latestNowMs);var latestMaxUntilMs=latestStartedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;var latestEffectiveUntilMs=Math.min(latestUntilMs,latestMaxUntilMs);if(isFinite(latestEffectiveUntilMs)&&latestEffectiveUntilMs>latestNowMs){return{active:true,requestId:latestRequestId,untilMs:latestEffectiveUntilMs,remainingMs:Math.max(0,latestEffectiveUntilMs-latestNowMs),startedAtMs:latestStartedAtMs,maxUntilMs:latestMaxUntilMs,ceilingRemainingMs:Math.max(0,latestMaxUntilMs-latestNowMs),ceilingReached:latestEffectiveUntilMs>=latestMaxUntilMs,reason:latestProps.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REASON)||'',requestedBy:latestProps.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUESTED_BY)||''};}return tfbInactivePause_();}
function tfbClaimManualPause_(reason) {var claim=tfbWithCoordinatorLock_('claim-manual-pause',function(){var props=tfbScriptProperties_();var nowMs=tfbNowMs_();var currentId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var currentUntilMs=Number(props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS)||0);var currentStartedAtMs=tfbManualPauseStartMsUnlocked_(props,currentId,nowMs);var currentMaxUntilMs=currentStartedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;var currentEffectiveUntilMs=Math.min(currentUntilMs,currentMaxUntilMs);if(isFinite(currentEffectiveUntilMs)&&currentEffectiveUntilMs>nowMs){return{claimed: false,requestId:currentId,untilMs:currentEffectiveUntilMs,startedAtMs:currentStartedAtMs,maxUntilMs:currentMaxUntilMs,reason:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REASON)||''};}tfbDeleteManualPauseUnlocked_(props);var requestId=tfbNewManualRequestId_();var state=tfbWriteManualPauseUnlocked_(props, reason, requestId);state.claimed=true;state.reason=String(reason||'manual-refresh');return state;});tfbRecordRefreshEvent_(claim.claimed?'manual-pause-claimed':'manual-pause-deduplicated','requestId='+String(claim.requestId||'')+' reason='+String(claim.reason||''));return claim;}
function tfbExtendManualPause_(reason, requestId) {var extended=tfbWithCoordinatorLock_('extend-manual-pause',function(){var props=tfbScriptProperties_();var currentId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';if(currentId&&currentId !== String(requestId || '')){return false;}var state=tfbWriteManualPauseUnlocked_(props, reason, requestId);return!state.expired;});tfbRecordRefreshEvent_(extended?'manual-pause-extended':'manual-pause-extension-skipped','requestId='+String(requestId||'')+' reason='+String(reason||''));return extended;}
function tfbRenewManualPause_(requestId) {var requestedId=String(requestId||'').trim();var outcome=tfbWithCoordinatorLock_('renew-manual-pause',function(){var props=tfbScriptProperties_();var currentId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';var nowMs=tfbNowMs_();if(!requestedId){return{renewed:false,status:'invalid_request_id',requestId:requestedId,currentId:currentId,cleared:false};}if(currentId!==requestedId){return{renewed:false,status:'stale_request_id',requestId:requestedId,currentId:currentId,cleared:false};}var rawUntilMs=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS)||'';var currentUntilMs=Number(rawUntilMs||0);var startedAtMs=tfbManualPauseStartMsUnlocked_(props,currentId,nowMs);var maxUntilMs=startedAtMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_MAX_TOTAL_MS;if(nowMs>=maxUntilMs){tfbDeleteManualPauseUnlocked_(props);return{renewed:false,status:'hard_ceiling_reached',requestId:requestedId,currentId:currentId,untilMs:nowMs,startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,ceilingRemainingMs:0,ceilingReached:true,cleared:true};}if(!isFinite(currentUntilMs)||currentUntilMs<=nowMs){tfbDeleteManualPauseUnlocked_(props);return{renewed:false,status:'idle_lease_expired',requestId:requestedId,currentId:currentId,untilMs:currentUntilMs,startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,ceilingRemainingMs:Math.max(0,maxUntilMs-nowMs),ceilingReached:false,cleared:true};}var untilMs=Math.min(nowMs+TFB_REFRESH_COORDINATOR_.MANUAL_PAUSE_TTL_MS,maxUntilMs);props.setProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_UNTIL_MS,String(untilMs));return{renewed:true,status:'renewed',requestId:requestedId,currentId:currentId,untilMs:untilMs,startedAtMs:startedAtMs,maxUntilMs:maxUntilMs,remainingMs:Math.max(0,untilMs-nowMs),ceilingRemainingMs:Math.max(0,maxUntilMs-nowMs),ceilingReached:untilMs>=maxUntilMs,cleared:false};});var eventName=outcome.renewed?'manual-pause-renewed':(outcome.status==='hard_ceiling_reached'||outcome.status==='idle_lease_expired'?'manual-pause-renewal-expired':'manual-pause-renewal-refused');tfbRecordRefreshEvent_(eventName,'status='+String(outcome.status||'')+' requestId='+requestedId+' currentId='+String(outcome.currentId||'')+' untilMs='+String(outcome.untilMs||0)+' maxUntilMs='+String(outcome.maxUntilMs||0));return outcome;}
function tfbClearManualPause_(reason, expectedRequestId, force) {var outcome=tfbWithCoordinatorLock_('clear-manual-pause',function(){var props=tfbScriptProperties_();var currentId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_REQUEST_ID)||'';if(!force&&currentId !== String(expectedRequestId || '')){return{cleared:false,currentId:currentId};}tfbDeleteManualPauseUnlocked_(props);return{cleared:true,currentId:currentId};});tfbRecordRefreshEvent_(outcome.cleared?'manual-pause-cleared':'manual-pause-clear-skipped',String(reason||'')+' expected='+String(expectedRequestId||'')+' current='+String(outcome.currentId||''));return outcome.cleared;}
function tfbAutomaticRefreshAllowed_(label) {var pause = tfbReadManualPause_();if(!pause.active){return true;}tfbRecordRefreshEvent_('automatic-skipped-for-manual',String(label||'')+' requestId='+pause.requestId+' remainingMs='+pause.remainingMs);return false;}
function tfbAutomaticYieldPoint_(label) {var pause = tfbReadManualPause_();if(!pause.active){return false;}tfbRecordRefreshEvent_('automatic-yielded-for-manual',String(label||'')+' requestId='+pause.requestId+' remainingMs='+pause.remainingMs);return true;}
function tfbRunAutomaticRefresh_(callback, label) {if(typeof callback!=='function'){throw new Error('tfbRunAutomaticRefresh_ requires a callback function');}if(!tfbAutomaticRefreshAllowed_(label)){return{status:'skipped_manual_priority',label:String(label||'')};}var lock=LockService.getScriptLock();if(!lock.tryLock(TFB_REFRESH_COORDINATOR_.AUTO_LOCK_WAIT_MS)){tfbRecordRefreshEvent_('automatic-lock-busy',String(label||''));return{status:'skipped_lock_busy',label:String(label||'')};}try{if(!tfbAutomaticRefreshAllowed_(label)){return{status:'skipped_manual_priority',label:String(label||'')};}tfbRecordRefreshEvent_('automatic-started',String(label||''));var result=callback(function(boundaryLabel){return tfbAutomaticYieldPoint_(boundaryLabel||label);});tfbRecordRefreshEvent_('automatic-finished',String(label||''));return result;}finally{lock.releaseLock();}}
function tfbConfiguredFunction_(propertyName, forbiddenName) {var handlerName=String(tfbScriptProperties_().getProperty(propertyName)||'').trim();if(!handlerName){throw new Error('Missing Script Property: '+propertyName);}if(handlerName===forbiddenName){throw new Error('Recursive refresh handler is not allowed: '+handlerName);}var fn=globalThis[handlerName];if(typeof fn!=='function'){throw new Error('Configured handler does not exist: '+handlerName);}return{name:handlerName,fn:fn};}
function tfbConfiguredManualHandler_() {return tfbConfiguredFunction_(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_HANDLER,'tfbManualRefresh');}
function tfbToast_(message, title, seconds) {try{SpreadsheetApp.getActiveSpreadsheet().toast(String(message||''),String(title||'Tadawul Fast'),Number(seconds||5));}catch(err){console.log('[REFRESH-COORDINATOR] toast unavailable: '+err);}}
function tfbFindTriggerByHandler_(handlerName) {var triggers=ScriptApp.getProjectTriggers();for(var i=0;i<triggers.length;i++){if(triggers[i].getHandlerFunction()===handlerName){return triggers[i];}}return null;}
function tfbEnsureOneShotTrigger_(handlerName, delayMs) {var existing=tfbFindTriggerByHandler_(handlerName);if(existing){return{created: false,triggerId:existing.getUniqueId()};}var trigger=ScriptApp.newTrigger(handlerName).timeBased().after(Math.max(60*1000,Number(delayMs||0))).create();return{created:true,triggerId:trigger.getUniqueId()};}
function tfbScheduleDeferredManual_() {var scheduled=tfbWithCoordinatorLock_('schedule-deferred-manual',function(){var result=tfbEnsureOneShotTrigger_('tfbManualRefreshDeferred_',TFB_REFRESH_COORDINATOR_.DEFERRED_DELAY_MS);tfbScriptProperties_().setProperty(TFB_REFRESH_COORDINATOR_.PROP_DEFERRED_TRIGGER_ID,String(result.triggerId||''));return result;});tfbRecordRefreshEvent_('manual-deferred','created='+scheduled.created+' triggerId='+scheduled.triggerId);return scheduled;}
function tfbRemoveOwnDeferredTrigger_() {var removed=tfbWithCoordinatorLock_('remove-deferred-manual',function(){var props=tfbScriptProperties_();var expectedId=props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_DEFERRED_TRIGGER_ID)||'';if(!expectedId){return false;}var triggers=ScriptApp.getProjectTriggers();for(var i=0;i<triggers.length;i++){if(triggers[i].getUniqueId() === expectedId){ScriptApp.deleteTrigger(triggers[i]);break;}}props.deleteProperty(TFB_REFRESH_COORDINATOR_.PROP_DEFERRED_TRIGGER_ID);return true;});return removed;}
function tfbScheduleAutomaticResume_() {var handlerName=String(tfbScriptProperties_().getProperty(TFB_REFRESH_COORDINATOR_.PROP_AUTO_RESUME_HANDLER)||'').trim();if(!handlerName){tfbRecordRefreshEvent_('automatic-resume-next-schedule','no one-shot handler');return{created: false,reason:'next_scheduled_trigger'};}if(handlerName==='tfbManualRefresh'||handlerName==='tfbManualRefreshDeferred_'){throw new Error('Invalid automatic resume handler: '+handlerName);}var scheduled=tfbWithCoordinatorLock_('schedule-automatic-resume',function(){return tfbEnsureOneShotTrigger_(handlerName,TFB_REFRESH_COORDINATOR_.AUTO_RESUME_DELAY_MS);});tfbRecordRefreshEvent_('automatic-resume-scheduled','handler='+handlerName+' created='+scheduled.created);return scheduled;}
function tfbLogCleanupFailure_(step, err) {console.error('[REFRESH-COORDINATOR] cleanup step failed: '+String(step||'unknown')+' | '+String(err&&err.stack||err));}
function tfbManualLeaseContext_(requestId) {var ownedId=String(requestId||'');return{requestId:ownedId,renew:function(boundaryLabel){var outcome=tfbRenewManualPause_(ownedId);tfbRecordRefreshEvent_(outcome&&outcome.renewed?'manual-handler-boundary-renewed':'manual-handler-boundary-renewal-failed','boundary='+String(boundaryLabel||'')+' requestId='+ownedId+' status='+String(outcome&&outcome.status||'unknown'));return outcome;}};}
function tfbManualHandlerNeedsContinuation_(result) {if(!result||typeof result!=='object')return false;var nextIndex=Number(result.nextIndex);var totalSymbols=Number(result.totalSymbols);if(!isFinite(nextIndex)||!isFinite(totalSymbols))return false;if(nextIndex<0||totalSymbols<=0||nextIndex>=totalSymbols)return false;return result.partial===true||result.paused===true;}
function tfbExecuteManualHandler_(sourceLabel, configured, requestId) {configured=configured||tfbConfiguredManualHandler_();requestId=String(requestId||tfbNewManualRequestId_());var lock=LockService.getScriptLock();if(!lock.tryLock(TFB_REFRESH_COORDINATOR_.MANUAL_LOCK_WAIT_MS)){try{tfbScheduleDeferredManual_();}catch(scheduleErr){try{tfbClearManualPause_('deferred-schedule-failed', requestId, false);}catch(clearErr){tfbLogCleanupFailure_('clear-after-deferred-schedule-failure',clearErr);}throw scheduleErr;}tfbToast_('Automatic refresh is finishing a safe step. Manual refresh has been queued.','Manual refresh queued',8);return{status:'queued',handler:configured.name,requestId:requestId};}var pauseCleared=false;var keepPauseForContinuation=false;try{var latestPause=tfbReadManualPause_();if(latestPause.active&&latestPause.requestId){requestId=latestPause.requestId;}tfbExtendManualPause_(String(sourceLabel||'manual')+':running',requestId);tfbRecordRefreshEvent_('manual-started',configured.name+' requestId='+requestId);tfbToast_('Automatic refresh paused. Manual refresh started.','Manual refresh',5);var leaseContext=tfbManualLeaseContext_(requestId);var result=configured.fn(leaseContext);SpreadsheetApp.flush();if(tfbManualHandlerNeedsContinuation_(result)){var renewal=leaseContext.renew('handler-checkpoint:'+String(result.nextIndex)+'/'+String(result.totalSymbols));if(!renewal||renewal.renewed!==true){throw new Error('Manual refresh checkpoint was saved but lease renewal failed: '+String(renewal&&renewal.status||'unknown'));}var continuation=tfbScheduleDeferredManual_();keepPauseForContinuation=true;tfbRecordRefreshEvent_('manual-continuation-queued','handler='+configured.name+' requestId='+requestId+' progress='+String(result.nextIndex)+'/'+String(result.totalSymbols)+' triggerId='+String(continuation.triggerId||''));tfbToast_('Checkpoint saved at '+String(result.nextIndex)+' / '+String(result.totalSymbols)+'. Continuation queued.','Manual refresh continuing',8);var continuationResult={};for(var resultKey in result){if(Object.prototype.hasOwnProperty.call(result,resultKey)){continuationResult[resultKey]=result[resultKey];}}continuationResult.coordinatorStatus='queued_continuation';continuationResult.handler=configured.name;continuationResult.requestId=requestId;continuationResult.renewal=renewal;continuationResult.continuation=continuation;return continuationResult;}tfbRecordRefreshEvent_('manual-finished',configured.name+' requestId='+requestId);tfbToast_('Manual refresh completed. Automatic refresh resumed.','Refresh complete',7);return result;}catch(err){tfbRecordRefreshEvent_('manual-failed',String(err&&err.stack||err));tfbToast_('Manual refresh failed: '+err,'Refresh error',10);throw err;}finally{try{lock.releaseLock();}catch(releaseErr){tfbLogCleanupFailure_('release-lock',releaseErr);}if(keepPauseForContinuation){tfbRecordRefreshEvent_('manual-cleanup-kept-for-continuation','requestId='+requestId);}else{try{pauseCleared=tfbClearManualPause_('manual-finally', requestId, false);}catch(clearErr){tfbLogCleanupFailure_('clear-manual-pause',clearErr);}}if(keepPauseForContinuation){}else if (pauseCleared){try{tfbRemoveOwnDeferredTrigger_();}catch(triggerErr){tfbLogCleanupFailure_('remove-deferred-trigger',triggerErr);}try{tfbScheduleAutomaticResume_();}catch(resumeErr){tfbLogCleanupFailure_('schedule-automatic-resume',resumeErr);}}else{tfbRecordRefreshEvent_('manual-cleanup-preserved-newer-request','requestId='+requestId);}}}
function tfbManualRefresh() {var configured = tfbConfiguredManualHandler_();var claim = tfbClaimManualPause_('menu-request');if(!claim.claimed){tfbToast_('A manual refresh is already queued or running.','Manual refresh already requested',6);return{status:String(claim.reason||'').indexOf(':running')>=0?'already_running':'already_queued',requestId:claim.requestId};}return tfbExecuteManualHandler_('menu',configured,claim.requestId);}
function tfbManualRefreshDeferred_() {var configured = tfbConfiguredManualHandler_();try{tfbRemoveOwnDeferredTrigger_();}catch(consumeErr){tfbLogCleanupFailure_('consume-deferred-trigger',consumeErr);}var pause = tfbReadManualPause_();if(!pause.active||!pause.requestId){tfbRecordRefreshEvent_('manual-deferred-expired','Consumed deferred trigger found no active owned pause; continuation stopped.');return{status:'expired_continuation',reason:'manual_pause_not_active'};}var requestId=pause.requestId;if(!tfbExtendManualPause_('deferred-request',requestId)){return{status:'expired_continuation',reason:'manual_pause_extension_failed',requestId:requestId};}return tfbExecuteManualHandler_('deferred',configured,requestId);}
function tfbManualRefreshStatus() {var pause = tfbReadManualPause_();var props=tfbScriptProperties_();var status={version:TFB_REFRESH_COORDINATOR_VERSION,pause:pause,manualHandler:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_MANUAL_HANDLER)||'',autoResumeHandler:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_AUTO_RESUME_HANDLER)||'',deferredTriggerId:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_DEFERRED_TRIGGER_ID)||'',lastEvent:props.getProperty(TFB_REFRESH_COORDINATOR_.PROP_LAST_EVENT)||''};console.log('[REFRESH-COORDINATOR-STATUS] '+JSON.stringify(status));return status;}
function tfbClearStaleManualRefreshPause() {try{tfbClearManualPause_('owner-emergency-clear','',true);}finally{try{tfbRemoveOwnDeferredTrigger_();}catch(err){tfbLogCleanupFailure_('emergency-remove-deferred-trigger',err);}}tfbToast_('Manual refresh pause cleared. Automatic refresh may continue.','Refresh control',6);}
