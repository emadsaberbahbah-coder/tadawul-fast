# ==========================================
# Tadawul Fast Bridge – Requirements
# Unified engine for 9 pages + Google Sheets
# ==========================================

# Web framework & ASGI server
fastapi
uvicorn[standard]

# HTTP / async clients (used by core.data_engine and routes)
httpx
aiohttp
requests

# Validation & settings (env.py, models, routes)
pydantic
pydantic-settings
python-dotenv

# Rate limiting (used in main.py via SlowAPI)
slowapi

# Google Sheets / Google APIs (google_sheets_service.py)
google-api-python-client
google-auth
google-auth-httplib2

# Utilities & robustness (can be used by data_engine, caching, retries)
cachetools
tenacity
