# ============================================================
# Tadawul Fast Bridge — Dependencies (v1.6.0)
# Target: Production (Render/Linux) + Local Development
#
# IMPORTANT (pip rules):
# - Only package specifiers are allowed (e.g., fastapi>=0.110.0)
# - Any explanation must start with "#" as a comment
# ============================================================

# --- Core Web Framework & Server ---
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0

# High-performance event loop (Linux only)
uvloop>=0.19.0; sys_platform != "win32"

# High-performance HTTP parser (Linux only)
httptools>=0.6.1; sys_platform != "win32"

# Request rate limiting
slowapi>=0.1.9,<1.0.0
limits>=3.7.0,<5.0.0

# --- Validation & Settings ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
typing-extensions>=4.10.0

# --- Financial Data & Analysis ---
yfinance>=0.2.37,<1.0.0
pandas>=2.2.1,<3.0.0
numpy>=1.26.4,<3.0.0
scipy>=1.12.0,<2.0.0

# --- HTTP Clients & Networking ---
httpx>=0.27.0,<1.0.0
requests>=2.31.0,<3.0.0
aiohttp>=3.9.3,<4.0.0

# --- Parsing & Scrapers (news / alternative data) ---
beautifulsoup4>=4.12.3,<5.0.0
lxml>=5.1.0,<6.0.0
html5lib>=1.1,<2.0.0

# --- Google Cloud / Sheets Integration ---
google-api-python-client>=2.122.0,<3.0.0
google-auth>=2.28.2,<3.0.0
google-auth-httplib2>=0.2.0,<1.0.0
google-api-core>=2.17.1,<3.0.0

# --- Utilities & Optimization ---
orjson>=3.9.15,<4.0.0
cachetools>=5.3.3,<6.0.0
psutil>=5.9.8,<7.0.0
