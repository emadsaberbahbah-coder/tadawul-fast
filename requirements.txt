# ============================================================
# Tadawul Fast Bridge — requirements.txt (FULL REPLACEMENT)
# Safe for Render + FastAPI + async fetchers + Google Sheets
# ============================================================

fastapi>=0.109.0
uvicorn[standard]>=0.27.0

python-dotenv>=1.0.0

# Rate limiting
slowapi>=0.1.9
limits>=3.7.0

# Pydantic (some modules may still rely on settings)
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Fast JSON + caching
orjson>=3.9.10
cachetools>=5.3.0,<6.0.0
typing-extensions>=4.9.0,<5.0.0

# HTTP clients
httpx>=0.27.0
requests>=2.31.0
aiohttp>=3.9.0

# HTML parsing (Argaam / scraping)
beautifulsoup4>=4.12.0
lxml>=5.1.0
html5lib>=1.1

# Market data
yfinance>=0.2.36

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Google APIs (Sheets read via service account)
google-api-python-client>=2.100.0,<3.0.0
google-auth>=2.20.0,<3.0.0
google-auth-httplib2>=0.2.0,<1.0.0
google-api-core>=2.0.0,<3.0.0
googleapis-common-protos>=1.0.0,<2.0.0
proto-plus>=1.22.0,<2.0.0
protobuf>=4.25.0,<6.0.0
httplib2>=0.22.0,<1.0.0
