# =============================================================================
# requirements.txt
# =============================================================================
# TADAWUL FAST BRIDGE - DEPENDENCY SPECIFICATION (Render-safe) (v7.5.1)
# =============================================================================
# Goal of this revision:
# - ✅ Remove hash-checking instability (NO --hash lines)
# - ✅ Remove Locust/dev-only deps from production build (was pulling wsproto stack)
# - ✅ Use maintained Google Translate client (py-googletrans) instead of risky pins
# - ✅ Keep core + ML stack; Deep Learning remains optional (see bottom)
#
# Notes:
# - DO NOT install with: pip install --require-hashes -r requirements.txt
# - If you want locked + hashed builds, use a separate requirements.lock generated on Linux.
# =============================================================================

# =============================================================================
# PROTOBUF & GRPC (CRITICAL ECOSYSTEM PINS)
# =============================================================================
protobuf==4.25.8
grpcio>=1.60.0,<1.65.0
grpcio-status>=1.60.0,<1.65.0
googleapis-common-protos>=1.62.0,<1.65.0
proto-plus>=1.23.0,<1.25.0

# =============================================================================
# CORE WEB FRAMEWORK (Pydantic V2)
# =============================================================================
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.32.0,<0.33.0
starlette>=0.41.0,<0.42.0
python-multipart>=0.0.12,<0.0.15
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.5.0,<3.0.0
pydantic-core>=2.23.0,<3.0.0
typing-extensions>=4.12.0,<5.0.0

# =============================================================================
# ASGI / WEB SERVERS / CONCURRENCY (PRODUCTION)
# =============================================================================
gunicorn>=23.0.0,<24.0.0
# Uvicorn docs recommend uvicorn-worker as replacement for deprecated uvicorn.workers
uvicorn-worker>=0.2.0,<0.4.0
anyio>=4.6.0,<5.0.0

# =============================================================================
# PERFORMANCE OPTIMIZATIONS (C/Rust Compiled)
# =============================================================================
uvloop>=0.21.0,<0.22.0; sys_platform != "win32"
httptools>=0.6.4,<0.7.0; sys_platform != "win32"
orjson>=3.10.7,<4.0.0
python-rapidjson>=1.18,<2.0
ciso8601>=2.3.1,<3.0.0; sys_platform != "win32"
aiodns>=3.2.0,<4.0.0; sys_platform != "win32"
rapidfuzz>=3.9.0,<4.0.0

# =============================================================================
# RATE LIMITING & SECURITY
# =============================================================================
slowapi>=0.1.9,<0.2.0
limits>=3.13.0,<4.0.0
PyJWT>=2.9.0,<3.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.2.0,<5.0.0
cryptography>=43.0.0,<44.0.0
pyOpenSSL>=24.2.0,<25.0.0

# =============================================================================
# HTTP CLIENTS & NETWORKING
# =============================================================================
httpx>=0.27.2,<0.28.0
httpcore>=1.0.5,<2.0.0
h2>=4.1.0,<5.0.0
requests>=2.32.0,<3.0.0
aiohttp>=3.10.0,<4.0.0
aiofiles>=24.1.0,<25.0.0
websockets>=13.0,<14.0
tenacity>=9.0.0,<10.0.0
backoff>=2.2.0,<3.0.0

# =============================================================================
# DATA PROVIDERS & FINANCIAL LIBRARIES
# =============================================================================
yfinance>=0.2.43,<0.3.0
pandas>=2.2.3,<3.0.0
numpy>=1.26.4,<2.0.0
scipy>=1.14.0,<2.0.0
pandas-market-calendars>=4.4.0,<5.0.0
exchange-calendars>=4.5.0,<5.0.0

# =============================================================================
# MACHINE LEARNING & FORECASTING (CPU-friendly)
# =============================================================================
scikit-learn>=1.5.0,<1.6.0
xgboost>=2.1.0,<2.2.0
lightgbm>=4.5.0,<5.0.0
statsmodels>=0.14.4,<0.15.0
prophet>=1.1.5,<1.2.0
hmmlearn>=0.3.2,<0.4.0
optuna>=4.0.0,<5.0.0
shap>=0.46.0,<0.47.0
joblib>=1.4.2,<2.0.0

# =============================================================================
# NLP, PARSING & DATA EXTRACTION
# =============================================================================
nltk>=3.9.0,<4.0.0
beautifulsoup4>=4.12.3,<5.0.0
lxml>=5.3.0,<6.0.0
feedparser>=6.0.11,<7.0.0

# IMPORTANT:
# Avoid googletrans pins that force very old httpx/httpcore.
# Use the maintained distribution:
py-googletrans>=4.0.0,<4.1.0

# =============================================================================
# GOOGLE CLOUD INTEGRATION & SHEETS
# =============================================================================
google-api-python-client>=2.140.0,<3.0.0
google-auth>=2.34.0,<3.0.0
google-auth-httplib2>=0.2.0,<0.3.0
google-api-core>=2.19.0,<3.0.0
google-cloud-secret-manager>=2.20.0,<3.0.0
gspread>=6.1.2,<7.0.0

# =============================================================================
# DATABASE & DISTRIBUTED CACHE INTEGRATIONS
# =============================================================================
redis[hiredis]>=5.0.8,<6.0.0
hiredis>=3.0.0,<4.0.0
aiomcache>=0.8.2,<1.0.0
aiocache[redis,memcached]>=0.12.2,<0.13.0
sqlalchemy>=2.0.35,<2.1.0
asyncpg>=0.29.0,<0.31.0

# =============================================================================
# MONITORING, TRACING & OBSERVABILITY
# =============================================================================
prometheus-client>=0.20.0,<0.22.0
opentelemetry-api>=1.27.0,<1.28.0
opentelemetry-sdk>=1.27.0,<1.28.0
opentelemetry-instrumentation-fastapi>=0.48b0,<0.50b0
opentelemetry-exporter-otlp>=1.27.0,<1.28.0
sentry-sdk[fastapi]>=2.13.0,<3.0.0
structlog>=24.4.0,<25.0.0
loguru>=0.7.2,<0.8.0

# =============================================================================
# UTILITIES & CONFIGURATION
# =============================================================================
python-dotenv>=1.0.1,<1.1.0
pyyaml>=6.0.2,<7.0.0
tomli>=2.0.1,<3.0.0; python_version < "3.11"

# =============================================================================
# OPTIONAL: DEEP LEARNING (commented out for Render stability)
# -----------------------------------------------------------------------------
# If you truly need Torch/Transformers in production:
# - Prefer CPU wheels to avoid downloading CUDA/NVIDIA stacks on Render.
# - E.g. move these to a separate requirements-ml.txt and install only when needed.
# =============================================================================
# torch>=2.4.0,<2.6.0; sys_platform != "win32"
# transformers>=4.44.0,<5.0.0
# pytorch-forecasting>=1.0.0,<2.0.0

# =============================================================================
# DEV / QA / LOAD TESTING (REMOVED from production requirements)
# -----------------------------------------------------------------------------
# locust, pytest, pytest-asyncio, pytest-cov, ruff were causing extra deps and
# increased install surface area (including the wsproto chain you crashed on).
# Keep them in a separate requirements-dev.txt instead.
# =============================================================================
