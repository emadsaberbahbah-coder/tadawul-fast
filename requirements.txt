# ==========================================
# Tadawul Fast Bridge – Requirements
# Unified engine for 9 pages + Google Sheets
# ==========================================

# Web framework & ASGI server
fastapi
uvicorn[standard]

# HTTP / async clients (used by core.data_engine, core.data_engine_v2, routes)
httpx
aiohttp
requests

# Validation & settings (env.py, models, routes)
pydantic
pydantic-settings
python-dotenv

# Rate limiting (used in main.py via SlowAPI)
slowapi

# Google Sheets / Google APIs (google_sheets_service.py)
google-api-python-client
google-auth
google-auth-httplib2

# Optional market data fallback (Yahoo Finance via v1 engine)
yfinance

# Utilities & robustness (caching, retries, etc.)
cachetools
tenacity
