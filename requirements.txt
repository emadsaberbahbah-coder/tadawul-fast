# =============================================================================
# Tadawul Fast Bridge - Production Requirements
# Version: 3.1.1
# Python: 3.11.9
# Target: Render (Web Service)
# =============================================================================

# -------------------------------
# Core Framework & ASGI Server
# -------------------------------
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# -------------------------------
# Data Models & Configuration
# -------------------------------
pydantic==2.5.3
pydantic-settings==2.1.0

# -------------------------------
# Google Services Integration
# (used by google_sheets_service, google_apps_script_client)
# -------------------------------
gspread==5.12.4
google-auth==2.23.3
google-api-python-client==2.108.0

# -------------------------------
# HTTP Clients & API Integration
# (used by main.py HTTPClient + other helpers)
# -------------------------------
requests==2.31.0
aiohttp==3.8.6
httpx==0.25.2

# -------------------------------
# Data Analysis & Processing
# (for advanced_analysis, exports, Sheets utilities)
# -------------------------------
pandas==2.1.4
numpy==1.24.4
openpyxl==3.1.2

# -------------------------------
# Web Scraping & Data Extraction
# (for Argaam/Tadawul HTML parsing if needed)
# -------------------------------
beautifulsoup4==4.12.2
lxml==4.9.3

# -------------------------------
# Error Handling & Resilience
# -------------------------------
tenacity==8.2.3

# -------------------------------
# Monitoring & Logging
# (structlog optional but kept for compatibility)
# -------------------------------
structlog==23.2.0
prometheus-client==0.19.0

# -------------------------------
# Environment & Configuration
# -------------------------------
python-dotenv==1.0.0
pyyaml==6.0.1

# -------------------------------
# API Rate Limiting & Security
# (used directly in main.py)
# -------------------------------
slowapi==0.1.9
limits==3.6.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# -------------------------------
# Performance & Utilities
# -------------------------------
orjson==3.9.10
python-dateutil==2.8.2
cachetools==5.3.2
pytz==2023.3.post1
tzdata==2023.4

# -------------------------------
# Security & SSL Enhancements
# -------------------------------
certifi==2023.11.17
urllib3==2.1.0
cryptography==41.0.7

# -------------------------------
# Data Validation & Sanitization
# -------------------------------
jsonschema==4.20.0
email-validator==2.1.1

# -------------------------------
# Database & ORM Support (OPTIONAL / FUTURE)
# - Not used by main.py, kept in case other modules rely on it.
#   Once we confirm it's unused, these two can be safely removed.
# -------------------------------
sqlalchemy==2.0.23
alembic==1.12.1

# -------------------------------
# Enhanced Caching (OPTIONAL / FUTURE)
# - Current cache in main.py is file-based TTLCache.
#   Keep redis for future distributed cache if needed.
# -------------------------------
redis==5.0.1

# -------------------------------
# System Monitoring (OPTIONAL)
# -------------------------------
psutil==5.9.6

# -------------------------------
# Low-level Dependencies / Compatibility
# (usually installed transitively, pinned here for stability)
# -------------------------------
charset-normalizer==3.3.2
idna==3.6
asgiref==3.7.2
sniffio==1.3.0
anyio==3.7.1
starlette==0.27.0
