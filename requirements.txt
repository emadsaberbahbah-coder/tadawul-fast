# --- Web API / Backend ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
slowapi>=0.1.9
python-dotenv>=1.0

# --- HTTP Clients (engine/providers usually need at least one) ---
httpx>=0.27
aiohttp>=3.10
requests>=2.32

# --- Performance / JSON (optional but recommended) ---
orjson>=3.10

# --- Google Sheets (service account) ---
google-api-python-client>=2.140
google-auth>=2.33
google-auth-httplib2>=0.2.0

# --- Common utilities (often pulled indirectly, but safe to include) ---
cachetools>=5.3
