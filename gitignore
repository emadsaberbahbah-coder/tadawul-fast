# Tadawul Fast Bridge — Git Ignore Rules (v1.7.0)
# =============================================================================
# PROD-SAFE EXCLUSION LIST
# Prevent secrets, local envs, caches, build outputs, and runtime artifacts
# from being committed.
#
# Notes
# - Keep `.env.example` tracked as a template.
# - Keep `.github/` tracked (workflows).
# - Add only what is safe to ignore globally (avoid ignoring real source).
# =============================================================================

# -----------------------------------------------------------------------------
# Python / FastAPI Core
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.pyd
*.so
*.dylib
.python-version
.ipynb_checkpoints/
.pyenv/
.venv.bak/

# -----------------------------------------------------------------------------
# Virtual Environments
# -----------------------------------------------------------------------------
venv/
.venv/
env/
ENV/
.ENV/
env.bak/
.conda/
conda/
pip-wheel-metadata/

# -----------------------------------------------------------------------------
# Packaging / Build / Distribution
# -----------------------------------------------------------------------------
build/
dist/
downloads/
eggs/
.eggs/
*.egg-info/
*.egg
wheels/
sdist/
develop-eggs/
parts/
var/
MANIFEST
.installed.cfg

# -----------------------------------------------------------------------------
# Logs / Runtime Artifacts
# -----------------------------------------------------------------------------
*.log
logs/
*.out
*.err
*.pid
*.pid.lock
*.prof
*.profile
celerybeat-schedule
celerybeat.pid

# -----------------------------------------------------------------------------
# Testing / Coverage / Tooling Caches
# -----------------------------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
.tox/
.nox/
.hypothesis/
.pyre/
.pytype/
.pylint.d/
.pyright/
.dmypy.json

# -----------------------------------------------------------------------------
# Local Databases / Snapshots / Exports
# -----------------------------------------------------------------------------
*.sqlite
*.sqlite3
*.db
*.dump
*.bak
*.swp
*.swo
*.tmp
*.temp
*.lock
*.lock.bak

.snapshots/
tmp/
cache/
exports/
reports/
artifacts/
data/

# Keep sample datasets if you use them:
# !data/samples/
# !data/README.md

# -----------------------------------------------------------------------------
# Security & Secrets (CRITICAL)
# -----------------------------------------------------------------------------
# Env files (track only `.env.example`)
.env
.env.*
!.env.example
.envrc
*.env
env.local
.env.local
.env.production.local
.env.development.local
.env.test.local

# Google Cloud / Service Account JSONs (Aggressive Guard)
credentials.json
service-account.json
service_account.json
google_sa.json
google_creds.json
sa_key.json
key.json
keys/*.json
secrets/*.json
private/*.json
token.json
client_secret.json
oauth*.json
gcp-*.json
*.json.key

# Generic secret patterns (use cautiously)
*.secret
*.secrets
*.pem
*.key
*.crt
*.pfx
*.p12
*.csr
*.der

# -----------------------------------------------------------------------------
# IDE / Editor / OS
# -----------------------------------------------------------------------------
# VS Code
.vscode/
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# JetBrains (PyCharm/IntelliJ)
.idea/
*.iws
*.iml
*.ipr

# macOS / Windows
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
Desktop.ini

# -----------------------------------------------------------------------------
# Node / Frontend (in case you have any tooling)
# -----------------------------------------------------------------------------
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.next/
.nuxt/
.svelte-kit/
.vite/
dist-ssr/

# -----------------------------------------------------------------------------
# Local Deployment / Platforms
# -----------------------------------------------------------------------------
.render/
.vercel/
.serverless/
.elasticbeanstalk/
firebase-debug.log*
.firebaserc

# Docker / Compose local overrides
docker-compose.override.yml
compose.override.yml

# -----------------------------------------------------------------------------
# Render / Gunicorn / Uvicorn runtime
# -----------------------------------------------------------------------------
gunicorn.conf.local.py
uvicorn.conf.local.py

# -----------------------------------------------------------------------------
# LLM / Copy-Paste Artifact Files (optional safety)
# -----------------------------------------------------------------------------
*.md.backup
*.txt.backup

# -----------------------------------------------------------------------------
# Do NOT ignore source code folders like:
# core/, routes/, schemas/, scripts/  (keep tracked)
# -----------------------------------------------------------------------------
