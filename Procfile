# Procfile (FULL REPLACEMENT)
# Tadawul Fast Bridge — PROD SAFE (workers + access log toggle)

web: sh -c 'set -eu; LOG_LEVEL_LC="$(printf "%s" "${LOG_LEVEL:-info}" | tr "[:upper:]" "[:lower:]")"; PORT_VAL="${PORT:-8000}"; KEEPALIVE_VAL="${UVICORN_KEEPALIVE:-75}"; GRACE_VAL="${UVICORN_GRACEFUL_TIMEOUT:-30}"; CONC="${WEB_CONCURRENCY:-1}"; ACCESS_RAW="$(printf "%s" "${UVICORN_ACCESS_LOG:-1}" | tr "[:upper:]" "[:lower:]")"; WORKERS=""; case "$CONC" in (""|*[!0-9]*) CONC="1";; esac; if [ "$CONC" -gt 1 ]; then WORKERS="--workers $CONC"; fi; ACCESS_FLAG="--access-log"; case "$ACCESS_RAW" in (0|false|no|off) ACCESS_FLAG="--no-access-log";; esac; exec uvicorn main:app --host 0.0.0.0 --port "$PORT_VAL" --proxy-headers --forwarded-allow-ips "*" --lifespan on --timeout-keep-alive "$KEEPALIVE_VAL" --graceful-timeout "$GRACE_VAL" --log-level "$LOG_LEVEL_LC" $ACCESS_FLAG $WORKERS --no-server-header'
